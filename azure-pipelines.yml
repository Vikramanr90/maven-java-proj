trigger:
  - master

pool: vikram-first-self-agent-pool

stages:
- stage: test_stage
  jobs: 
    - job: test_jobs
      steps:
      - bash: echo "hello world by vikram"
- stage: maven_build_stage
  jobs:
    - job: maven_build_job
      steps:
        - template: maven_build_job.yml
          parameters:
            maven_pom_file_location: "MyWebApp/pom.xml"